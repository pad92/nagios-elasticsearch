language: python
os: linux
dist: xenial

python:
  - "2.7"
before_install:
  - python --version
  - pip install -U pip
  - pip install codecov
install:
  - pip install -r requirements.txt
script:
  - python setup.py test
after_success:
  - codecov
